from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='To determine which products customers buy on a cyclical basis. A model that predicts these cycles for all relevant products and customer groups.',
    author='Simran',
    license='MIT',
)
